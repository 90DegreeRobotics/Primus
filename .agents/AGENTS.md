# Local Planning Reminder

The authoritative contributor law is the repository root `AGENTS.md`.

- When the operator or active IDE/harness mode asks for plan-only work, stop at
  the plan stage and wait for explicit manual authorization before modifying
  code.
- Markdown is guidance, not a mechanical brake. The actual hard stop must come
  from IDE/harness plan mode or an equivalent hook.
